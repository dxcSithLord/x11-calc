name: C/C++ CI

on:
  push:
    branches: [ unstable ]

  pull_request:
    branches: [ unstable ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    
    - name: make all
      run:  make 
